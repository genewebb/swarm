{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "reviewer.schema.json",
  "title": "Reviewer Result",
  "description": "Schema for reviewer.result.json produced by the reviewer subagent",
  "type": "object",
  "required": [
    "schemaVersion",
    "subagent",
    "runId",
    "handoffId",
    "verdict",
    "summary",
    "reviewedFiles",
    "issues",
    "createdAt"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": {
      "type": "string",
      "const": "1.0",
      "description": "Schema version"
    },
    "subagent": {
      "type": "string",
      "const": "reviewer",
      "description": "Identifies the producing agent"
    },
    "runId": { "type": "string", "description": "The run id from the handoff" },
    "handoffId": {
      "type": "string",
      "description": "The handoff this result responds to"
    },
    "verdict": {
      "type": "string",
      "enum": ["approved", "rejected"],
      "description": "Approved if implementation conforms to standards; rejected otherwise"
    },
    "summary": {
      "type": "string",
      "description": "Brief summary of the review"
    },
    "reviewedFiles": {
      "type": "array",
      "description": "Files that were reviewed",
      "items": { "type": "string" }
    },
    "issues": {
      "type": "array",
      "description": "List of issues found; empty if approved",
      "items": {
        "type": "object",
        "required": ["description"],
        "properties": {
          "description": { "type": "string" },
          "file": { "type": "string" },
          "line": { "type": "integer" },
          "standard": { "type": "string" }
        }
      }
    },
    "notes": {
      "type": "string",
      "description": "Optional notes or recommendations"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the review was completed"
    }
  }
}
